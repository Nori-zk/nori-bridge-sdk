{
  "name": "@nori-zk/contracts-ethereum",
  "version": "0.1.0",
  "description": "NoriTokenBridge contract",
  "main": "index.js",
  "scripts": {
    "build": "ETH_NETWORK=hardhat npm run clean && ETH_NETWORK=hardhat hardhat compile",
    "test": "ETH_NETWORK=hardhat hardhat test",
    "test-ci": "ETH_NETWORK=hardhat hardhat test",
    "clean": "hardhat clean",
    "deploy": "dotenv -e .env -- sh -c 'npx hardhat run bin/deploy.ts --network $ETH_NETWORK'",
    "test:lock": "dotenv -e .env -- sh -c 'npx hardhat run bin/lock.ts --network $ETH_NETWORK'"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.9",
    "@nomicfoundation/hardhat-ethers": "^3.0.9",
    "@nomicfoundation/hardhat-ignition-ethers": "^0.15.13",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.13",
    "@nomicfoundation/hardhat-verify": "^2.0.14",
    "@typechain/ethers-v6": "^0.5.1",
    "@typechain/hardhat": "^9.1.0",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "dotenv-cli": "^8.0.0",
    "hardhat": "^2.24.0",
    "hardhat-gas-reporter": "^1.0.10",
    "solidity-coverage": "^0.8.16",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.5.0"
  }
}
