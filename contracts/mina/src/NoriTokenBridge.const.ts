import { Field } from 'o1js';

/** Maximum number of deposit roots kept in the action window. */
export const maxWindow = 32;

/** Minimum bridge burn amount */
export const minBridgeBurnAmount = new Field(100);
